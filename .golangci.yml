issues:
  exclude-rules:
    - path: ".*\\/main_test.go$"
      linters:
        - exhaustruct
        - tparallel
        - paralleltest
linters:
  disable:
    - depguard
    - gci
    - varnamelen
    - wrapcheck
    - funlen
    - lll
    - dupl
    - ireturn
    - godot
    - tenv
  enable-all: true
